import { BirthYearPipe } from './birth-year.pipe';

describe('BirthYearPipe', () => {
  it('create an instance', () => {
    const pipe = new BirthYearPipe();
    expect(pipe).toBeTruthy();
  });
});
