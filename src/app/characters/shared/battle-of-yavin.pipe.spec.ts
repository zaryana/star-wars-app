import { BattleOfYavinPipe } from './battle-of-yavin.pipe';

describe('BattleOfYavinPipe', () => {
  it('create an instance', () => {
    const pipe = new BattleOfYavinPipe();
    expect(pipe).toBeTruthy();
  });
});
